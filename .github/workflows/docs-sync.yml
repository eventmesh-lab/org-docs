name: Sync aggregated docs

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deshabilitado temporalmente
        run: |
          echo "El flujo de sincronización externa está deshabilitado temporalmente."
          echo "Para restaurarlo, recupera la versión previa de este archivo desde el control de versiones."
