name: Build and Deploy aggregated docs

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deshabilitado temporalmente
        run: |
          echo "El flujo de agregado y despliegue externo está deshabilitado temporalmente."
          echo "Cuando desees reactivarlo, restaura este archivo desde el historial de Git."